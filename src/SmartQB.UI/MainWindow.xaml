<Window x:Class="SmartQB.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartQB.UI"
        xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
        xmlns:views="clr-namespace:SmartQB.UI.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="SmartQB" Height="768" Width="1024" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ImportViewModel}">
            <views:ImportView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
            <views:LibraryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ExportViewModel}">
            <views:ExportView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="SmartQB" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,0,0,5" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="{Binding Version, StringFormat=v{0}}" Style="{StaticResource MaterialDesignCaptionTextBlock}" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="{DynamicResource MaterialDesign.Brush.Foreground.Hint}"/>

                <Button Content="Import PDFs" Command="{Binding NavigateCommand}" CommandParameter="Import"
                        Margin="10,5" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Height="40"/>
                <Button Content="Question Library" Command="{Binding NavigateCommand}" CommandParameter="Library"
                        Margin="10,5" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Height="40"/>
                <Button Content="Export Paper" Command="{Binding NavigateCommand}" CommandParameter="Export"
                        Margin="10,5" Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Height="40"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="20"/>
    </Grid>
</Window>