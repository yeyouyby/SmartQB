<Window x:Class="SmartQB.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartQB.UI"
        xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
        xmlns:views="clr-namespace:SmartQB.UI.Views"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="SmartQB" Height="700" Width="1000" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:IngestionViewModel}">
            <views:IngestionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:QuestionBankViewModel}">
            <views:QuestionBankView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PaperCompositionViewModel}">
            <views:PaperCompositionView/>
        </DataTemplate>

        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2C3E50">
            <StackPanel>
                <TextBlock Text="SmartQB" Foreground="White" FontSize="28" FontWeight="Bold" Margin="20,30,20,30" HorizontalAlignment="Center"/>

                <Button Content="📥  Ingestion" Command="{Binding NavigateCommand}" CommandParameter="Ingestion" Style="{StaticResource MenuButton}"/>
                <Button Content="📚  Question Bank" Command="{Binding NavigateCommand}" CommandParameter="QuestionBank" Style="{StaticResource MenuButton}"/>
                <Button Content="📝  Composition" Command="{Binding NavigateCommand}" CommandParameter="Composition" Style="{StaticResource MenuButton}"/>

                <TextBlock Text="{Binding Version, StringFormat='v{0}'}" Foreground="#7F8C8D" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,50,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Background="White"/>
    </Grid>
</Window>
