<UserControl x:Class="SmartQB.UI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0" Margin="0,0,10,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="All Questions" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListBox ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Content}" TextTrimming="CharacterEllipsis" MaxHeight="40" Margin="5"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Padding="10">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="Question Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SelectedQuestion.Content}" TextWrapping="Wrap" Margin="0,0,0,20"/>

                    <TextBlock Text="Logic Path:" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="{Binding SelectedQuestion.LogicDescriptor}" TextWrapping="Wrap" Foreground="#4B5563"/>

                    <TextBlock Text="Difficulty:" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="{Binding SelectedQuestion.Difficulty}" Foreground="#4B5563"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>