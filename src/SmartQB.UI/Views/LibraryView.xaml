<UserControl x:Class="SmartQB.UI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
             xmlns:helpers="clr-namespace:SmartQB.UI.Helpers"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0" Margin="0,0,16,0" Padding="0,0,16,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="All Questions" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,16"/>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Search questions..." Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignOutlinedTextBox}" />
                    <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource MaterialDesignFlatButton}" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Filter by Tag: " VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource MaterialDesignBody1TextBlock}" />
                    <ComboBox ItemsSource="{Binding Tags}" SelectedItem="{Binding SelectedTag}" materialDesign:HintAssist.Hint="Select a tag" Width="150" DisplayMemberPath="Name" Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    <Button Content="Clear" Command="{Binding ClearFilterCommand}" Margin="16,0,0,0" Style="{StaticResource MaterialDesignOutlinedButton}" />
                </StackPanel>
                <ListBox ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}" Style="{StaticResource MaterialDesignChoiceChipPrimaryListBox}" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,4">
                                <WrapPanel helpers:MathRenderingHelper.MathText="{Binding Content}" />
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Padding="16" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="8">
            <wv2:WebView2 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" x:Name="DetailsWebView" />
        </Border>
    </Grid>
</UserControl>