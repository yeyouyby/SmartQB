<UserControl x:Class="SmartQB.UI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
             xmlns:helpers="clr-namespace:SmartQB.UI.Helpers"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0" Margin="0,0,10,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="All Questions" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,10,0" Padding="2" />
                    <Button Content="Search" Command="{Binding SearchCommand}" Padding="10,2" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Filter by Tag: " VerticalAlignment="Center" Margin="0,0,5,0" />
                    <ComboBox ItemsSource="{Binding Tags}" SelectedItem="{Binding SelectedTag}" Width="150" DisplayMemberPath="Name" />
                    <Button Content="Clear" Command="{Binding ClearFilterCommand}" Margin="10,0,0,0" Padding="10,2" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}" Foreground="Green" Margin="0,0,0,10" />
                <ListBox ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel helpers:MathRenderingHelper.MathText="{Binding Content}" Margin="5" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Padding="10">
            <wv2:WebView2 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" x:Name="DetailsWebView" />
        </Border>
    </Grid>
</UserControl>