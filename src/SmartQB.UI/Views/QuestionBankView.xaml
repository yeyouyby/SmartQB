<UserControl x:Class="SmartQB.UI.Views.QuestionBankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SmartQB.UI.Views"
             xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:QuestionBankViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <StackPanel Orientation="Horizontal" Margin="20" Grid.Row="0">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="30" VerticalContentAlignment="Center" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Search" Command="{Binding SearchCommand}" Width="100" Height="30"/>
        </StackPanel>

        <!-- Results -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}" AutoGenerateColumns="False" IsReadOnly="True" Margin="20,0,20,0" BorderThickness="1" BorderBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Content" Binding="{Binding Content}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="80"/>
                <DataGridTextColumn Header="Logic" Binding="{Binding LogicDescriptor}" Width="150"/>
                 <DataGridTemplateColumn Header="Tags" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                             <ItemsControl ItemsSource="{Binding Tags}">
                                 <ItemsControl.ItemTemplate>
                                     <DataTemplate>
                                         <Border Background="#EEE" CornerRadius="4" Margin="2" Padding="4,2">
                                             <TextBlock Text="{Binding Name}" FontSize="10"/>
                                         </Border>
                                     </DataTemplate>
                                 </ItemsControl.ItemTemplate>
                                 <ItemsControl.ItemsPanel>
                                     <ItemsPanelTemplate>
                                         <WrapPanel/>
                                     </ItemsPanelTemplate>
                                 </ItemsControl.ItemsPanel>
                             </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="Add to Paper Basket" Command="{Binding AddToBasketCommand}" Width="150" Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedQuestion}" Value="{x:Null}">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
