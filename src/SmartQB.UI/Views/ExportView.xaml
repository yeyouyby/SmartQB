<UserControl x:Class="SmartQB.UI.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SmartQB.UI.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ExportViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Export Paper" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <CheckBox x:Name="IncludeAnswersCheck" Content="Include Answers (Teacher Edition)" VerticalAlignment="Center" Margin="0,0,20,0" IsChecked="True"/>
            <Button Content="Preview HTML" Click="Preview_Click" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Export to PDF" Click="Export_Click" Padding="10,5"/>
            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="20,0,0,0" Foreground="Gray"/>
        </StackPanel>

        <!-- Hidden WebView for generating PDF, or shown for preview -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="10">
            <wv2:WebView2 x:Name="WebView" />
        </Border>
    </Grid>
</UserControl>